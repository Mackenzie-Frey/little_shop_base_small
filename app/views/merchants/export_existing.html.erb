<% binding.pry %>
<% @merchant.existing_users.each do |user| %>
  <%= user.name %>
  <%= user.email %>
  <%= user.user_revenue_by_merchant(@merchant) %>
  <%= user. %>
<% end %>

<!-- Test this is displaying to the page.
Integrate the weird spacing suggested in slack. -->
